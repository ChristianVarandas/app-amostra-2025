import 'package:helloworld/telaInicio.dart';
import 'package:flutter/material.dart';

class OrdenarCena extends StatelessWidget {
  const OrdenarCena({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      initialRoute: '/',
      routes: {
        '/': (_) => telaInicio(),
      },
    );
  }
}
